# Treat compressed data + binary docs as binary so git never applies
# line-ending / text normalisation (prevents cross-platform corruption).
*.gz  binary
*.pdf binary
*.ods binary
*.shp binary
*.shx binary
*.dbf binary
